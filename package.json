{
  "name": "events",
  "version": "0.1.0",
  "bin": {
    "events": "bin/events.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "compile": "tsc",
    "clear": "rm -rf dist cdk.out",
    "deploy": "export AWS_PROFILE=personal && npm run clear && npm run compile && cdk deploy",
    "bootstrap": "export AWS_PROFILE=personal && npm run clear && npm run compile && cdk bootstrap",
    "destroy": "export AWS_PROFILE=personal && npm run clear && npm run compile && cdk destroy"
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "@types/node": "10.17.27",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "aws-cdk": "2.5.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "aws-cdk-lib": "2.5.0",
    "aws-sdk": "^2.1124.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.16"
  }
}
